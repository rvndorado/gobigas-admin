import React, { Fragment } from 'react';
import LoginForm from '../containers/signIn/loginForm/loginForm';

const SignIn = () => {
    return(<Fragment>
        <LoginForm />
    </Fragment>);
}

export default SignIn;